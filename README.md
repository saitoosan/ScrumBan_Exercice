# Personal Finance Tracker — Correction

## Lancer l'application
```bash
python tracker.py
```

## Structure du projet
```
finance_tracker/
├── tracker.py      # Code principal (tout en un fichier)
└── data.json       # Généré automatiquement au premier Save & Exit
```

## Sprints
- **Sprint 1** : `add_income()`, `add_expense()`, `show_income()`, `show_expenses()`
- **Sprint 2** : `load_data()`, `save_data()`, `show_summary()`
- **Sprint 3** : `set_budget()`, `check_budget()`, `show_budgets()`, `show_savings()`
